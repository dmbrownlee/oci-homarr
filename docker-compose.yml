---
services:
  homarr:
    container_name: homarr
    image: ghcr.io/homarr-labs/homarr:latest
    restart: unless-stopped
    environment:
      - SECRET_ENCRYPTION_KEY=${KEY}
    volumes:
      - /appdata/${COMPOSE_PROJECT_NAME}/appdata:/appdata
    ports:
      - '7575:7575'
